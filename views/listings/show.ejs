<%- include('../partials/header.ejs') %>
    <!--<%= list.title %>
    <ul>
        <li><%=list.description%></li>
        <li><%=list.country%></li>
        <li><%=list.location%></li>
        <li> &#8377; <%=list.price.toLocaleString("en-IN")%></li>
    </ul>
    <a href="/listing/<%= list._id %>/edit"><button>Edit</button></a>
    <br><br>
    <form action="/listing/<%= list._id %>/delete?_method=DELETE" method="post">
        <button>Delete</button>
    </form>-->
    <div class=" flex flex-wrap px-10 h-screen items-center justify-center">

        <div class="card rounded-2xl overflow-hidden w-[700px] h-[80%] shadow-lg  mb-3">
            <img src="<%=list.image.url %>"
                class="w-[100%] h-80 object-cover object-center obj rounded-2xl" alt="picture">
            <div class="p-4">
                <!--<a href="/listing/<%= list._id %>"><li><%= list.title %></li></a>-->
                <p class="font-semibold">
                    <%= list.title %>
                </p>
                <p>
                    <%= list.description %>
                </p>
                <p>
                    <%= list.country %>
                </p>
                <p>
                    <%= list.location %>
                </p>
                <p>&#8377;<%= list.price.toLocaleString("en-IN") %>/night</p><br>
                <div class="flex gap-5">
                    <a href="/listing/<%= list._id %>/edit"><button class="bg-black p-2 px-8 rounded hover:cursor-pointer hover:bg-white hover:text-black hover:border-1 text-white">Edit</button></a>
                    
                    <form action="/listing/<%= list._id %>/delete?_method=DELETE" method="post">
                        <button class="bg-red-500 p-2 px-8 rounded text-white hover:cursor-pointer hover:bg-white hover:text-black hover:border-1">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <%- include('../partials/footer.ejs') %>